# ISSUE · Order Draft V2

**สถานะ:** เปิดไว้ · ยังไม่แก้โค้ด · **ห้ามรวมเข้า commit k190/k190b**
**เปิดเมื่อ:** 5 ส.ค. 2569 · พบตอนทำ Stock Claim Gate (k190b)
**ขอบเขต:** การอ่าน "รายการที่ลูกค้าสั่ง" เพื่อสร้างข้อความทวนรายการ

---

## ที่มา

k190b ตัดสินค้าที่ `stock = 0` ออกจากบรรทัดทวนรายการได้แล้ว และคิดยอดรวมใหม่จากรายการที่ซื้อได้เท่านั้น
ระหว่างทำพบข้อจำกัด 2 ข้อที่ **ยังไม่แก้ในรอบนี้** เพราะอยู่นอกขอบเขต "Stock Claim Gate"

---

## ข้อจำกัดที่ 1 — ลูกค้าไม่บอกจำนวน ระบบถือว่า 1

```js
const qm = after.match(/(\d{1,3})/);
out.push({ b, qty: qm ? parseInt(qm[1], 10) : 1 });
```

ถ้าลูกค้าพิมพ์ `เอาสับปะรดกับมะนาว` (ไม่ระบุจำนวน) ระบบจะทวนว่า `สับปะรด 1 และ มะนาว 1 รวม 2 ชิ้น`

**ทำไมตอนนี้ยังยอมรับได้:** ตรงกับพฤติกรรมแอดมินจริง — จากแชทจริง 32,427 บท แอดมินถามจำนวนเพียง **21.9% ของบท** ที่เหลือถือว่า 1 แล้วทวนให้ลูกค้าค้าน

**ความเสี่ยง:** ถ้าลูกค้าตั้งใจสั่ง 5 ชิ้นแต่พิมพ์สั้น ระบบทวนเป็น 1 → ลูกค้าอาจไม่ทันสังเกต → ได้ของน้อยกว่าที่ตั้งใจ

**แนวทางที่เสนอ (ยังไม่ทำ):** ถ้าอ่านจำนวนไม่ได้ ให้ทวนแบบไม่ระบุตัวเลข เช่น `รับสับปะรด กับ มะนาว อย่างละกี่ชิ้นดีคะ` แทนการเดาว่า 1

---

## ข้อจำกัดที่ 2 — ดึงรายการจากเทิร์นเดียว ไม่ใช่ทั้งบทสนทนา

`stockClaimGate` อ่านคู่ (กลิ่น, จำนวน) จาก **ข้อความล่าสุดของลูกค้า** เป็นหลัก
ถ้าเทิร์นล่าสุดไม่มีชื่อกลิ่นเลย (เช่น ลูกค้าพิมพ์แค่ `เอาตามนั้นเลย`) จะถอยไปอ่านจากบรรทัดที่ AI เขียน — ซึ่งอาจตกรายการไปแล้ว

**เคสที่จะพลาด**

| เทิร์น | ลูกค้าพิมพ์ |
|---|---|
| 1 | `เอาสับปะรด 1` |
| 2 | `เพิ่มมะนาว 2` |
| 3 | `เอาตามนั้นเลย` ← เทิร์นนี้ไม่มีชื่อกลิ่น |

ถ้าด่านต้องแก้บรรทัดทวนในเทิร์นที่ 3 จะอ่านรายการจากบรรทัดที่ AI เขียน ไม่ใช่จากที่ลูกค้าสั่งสะสมมา

**แนวทางที่เสนอ (ยังไม่ทำ):** ทำ order draft ที่สะสมข้ามเทิร์นจริงๆ (เก็บใน KV `draft:<shop>:<uid>`) แล้วให้ทั้งด่านตรวจสต็อกและการออกการ์ดอ่านจากที่เดียวกัน

⚠️ นี่คือของเดียวกับ **Conversation Brain Lite ระยะที่ 2** (Slot Engine เต็มรูปแบบ) ที่เคยออกแบบไว้ — ไม่ควรทำแยก

---

## เกณฑ์ว่างานนี้เสร็จ

- [ ] อ่านจำนวนไม่ได้ → ถามกลับ ไม่เดาว่า 1
- [ ] มี order draft ที่สะสมข้ามเทิร์น อ่านจากที่เดียวทั้งระบบ
- [ ] เทิร์นที่ไม่มีชื่อกลิ่น (`เอาตามนั้นเลย`) ยังทวนรายการได้ครบ
- [ ] มีเทสครอบ: สั่งข้าม 3 เทิร์น · ไม่ระบุจำนวน · เปลี่ยนใจกลางทาง
- [ ] ไม่ทำให้ k190/k190b ถอยหลัง (583 เทสเดิมต้องเขียว)

---

## สิ่งที่ **ไม่** อยู่ในขอบเขต Issue นี้

- การตรวจสถานะสต็อก (เสร็จแล้วใน k190/k190b)
- การคิดเงิน · Payment · KV config · Menu config
- prompt
