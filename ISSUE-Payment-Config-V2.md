# ISSUE · Payment Config V2

**สถานะ:** เปิดไว้ · ยังไม่แก้โค้ด · **เป็นงานหลังเปิดใช้งานจริง**
**เปิดเมื่อ:** 5 ส.ค. 2569 · พบตอนตรวจ Architecture ก่อน commit k188
**ขอบเขต:** เฉพาะ Store-specific Data (ข้อมูลการรับเงินของแต่ละร้าน) ไม่เกี่ยวกับ Business Logic

---

## สรุปปัญหา

หลังบ้านมีช่องให้แอดมินกรอกข้อมูลรับเงิน **ช่องเดียว เป็นข้อความอิสระ** ต่อ 1 ร้าน
โค้ดต้องเดาโครงสร้างเอาเองด้วย heuristic ตอนสร้างการ์ดชำระเงิน

ที่ 1 ร้านไม่มีปัญหา เพราะกรอกฟอร์แมตเดียวสม่ำเสมอ
ที่ **100 ร้าน** จะมี 100 ฟอร์แมต — heuristic จะเดาผิดแน่นอน

---

## จุดที่เกี่ยวข้องในโค้ด

| ตำแหน่ง | หน้าที่ |
|---|---|
| `payOf(env, shopId)` (~บรรทัด 123) | อ่านข้อความอิสระจาก KV `pay:<shopId>` · fallback เป็น Secret `PAY_<SHOP>` · แคช 60 วิ |
| `sendPayInfo()` (~บรรทัด 163) | แยกข้อความอิสระเป็น ธนาคาร / เลขบัญชี / ชื่อบัญชี ด้วย regex |
| `payFlex()` (~บรรทัด 7570) | สร้างการ์ด · ปุ่ม clipboard ผูกกับ `acctNo` ตัวเดียว |
| หลังบ้าน `/ctl` (~บรรทัด 3426, 3431) | อ่าน/เขียนช่องข้อมูลรับเงินต่อร้าน |

**สิ่งที่ทำงานถูกต้องอยู่แล้ว ไม่ต้องแก้:**
- แยกข้อมูลรายร้านจริง (KV key แยกต่อ `shopId` · แคชแยก key · ไม่มีข้อมูลข้ามร้าน)
- เปลี่ยนข้อมูลได้จากหลังบ้านโดยไม่ต้อง deploy
- ไม่มีเลขบัญชี/ชื่อบัญชีของร้านใดอยู่ใน source code, prompt, test หรือเอกสาร (ตรวจแล้ว 5 ส.ค. 2569)

---

## สิ่งที่ยังไม่รองรับ

### 1. หลายบัญชีต่อร้าน — ❌ ไม่รองรับ

```js
const acctNo = (pay.match(/\d[\d\- ]{5,}\d/) || [""])[0].replace(/\s/g, "");
```

ดึง**เลขชุดแรกที่เจอตัวเดียว** ถ้าร้านกรอก 3 ธนาคาร ลูกค้าจะเห็นแค่ธนาคารเดียวเสมอ
ร้านที่สลับบัญชีตามรอบ (กันยอดกระจุก) ทำไม่ได้เลย

**สิ่งที่ต้องมี:** โครงสร้างรองรับหลายบัญชี + กติกาว่าจะโชว์ใบไหน (ทั้งหมด / ตามลำดับ / สุ่ม / ตามยอด)

---

### 2. พร้อมเพย์ — ❌ ไม่รองรับ

ไม่มี field ไม่มี logic ไม่มีที่แสดงผล
ปุ่ม "📋 คัดลอกเลขบัญชี" ผูกกับ `acctNo` อย่างเดียว

**ความเสี่ยงตอนนี้:** ถ้าร้านกรอกเบอร์พร้อมเพย์ไว้**บรรทัดเหนือ**เลขบัญชี regex จะจับเบอร์พร้อมเพย์เป็น `acctNo`
→ การ์ดโชว์เบอร์เป็นเลขบัญชี และปุ่มคัดลอกก็คัดลอกผิด

---

### 3. QR — ❌ ไม่รองรับ

`payFlex()` ไม่มี image component เลย ใส่ QR ไม่ได้ทั้งแบบรูปนิ่งและแบบสร้างตามยอด

---

### 4. การ parse แบบ heuristic — ⚠️ เดาผิดได้

```js
bankName = บรรทัดแรกที่มีคำว่า ธนาคาร|bank|kbank|กสิกร|กรุง|ไทยพาณิชย์|scb|ktb|bbl|ออมสิน|ทหารไทย|ttb|uob|ยูโอบี
           ถ้าไม่เจอ → ใช้บรรทัดที่ 1
owner    = บรรทัดที่มีคำว่า ชื่อ|นาย|นาง|น.ส|หจก|บจก|บริษัท|ร้าน
           ถ้าไม่เจอ → ใช้บรรทัดสุดท้าย
```

**เคสที่จะพังจริง**

| ร้านกรอกแบบนี้ | การ์ดจะโชว์ |
|---|---|
| บรรทัดสุดท้ายเป็น `โอนแล้วส่งสลิปด้วยนะคะ` | ชื่อบัญชี = "โอนแล้วส่งสลิปด้วยนะคะ" |
| บรรทัดแรกเป็นหัวข้อ เช่น `ช่องทางชำระเงิน` | ธนาคาร = "ช่องทางชำระเงิน" |
| ใช้ธนาคารนอกลิสต์ (กรุงศรี/BAY · CIMB · TISCO · LH · เกียรตินาคิน) | ไม่เจอชื่อธนาคาร → ตกไปใช้บรรทัดที่ 1 |
| มีเบอร์โทรหรือวันที่อยู่ก่อนเลขบัญชี | เลขบัญชีผิด + ปุ่มคัดลอกผิด |

**⚠️ ข้อควรรู้:** heuristic ชุดนี้มีมาตั้งแต่ k169 แต่เดิมยิงเฉพาะตอนลูกค้าขอเลขบัญชี
**k188 ทำให้ยิงทุกออเดอร์** — ร้านที่กรอกผิดฟอร์แมตจะแสดงผิดกับลูกค้า**ทุกคน** ไม่ใช่นานๆ ครั้ง
ความถี่ของข้อผิดพลาดเพิ่มขึ้น แต่ต้นเหตุไม่ได้เกิดจาก k188

---

### 5. แยก field ธนาคาร / เลขบัญชี / ชื่อบัญชี — ❌ ยังไม่แยก

ทั้งหมดอยู่ในช่องข้อความเดียว → ตรวจสอบความถูกต้องตอนกรอกไม่ได้เลย
ไม่มีการเช็คว่าเลขบัญชีมีกี่หลัก ไม่มีการเช็คว่าเว้นช่องไหนไว้

---

## แนวทางที่เสนอ (ยังไม่ทำ — รอตัดสินใจหลังเปิดร้าน)

### หลังบ้าน — เปลี่ยนจากช่องเดียวเป็นฟอร์ม

```
บัญชีที่ 1   [ธนาคาร ▾] [เลขบัญชี] [ชื่อบัญชี]  ☑ ใช้งาน
บัญชีที่ 2   [ธนาคาร ▾] [เลขบัญชี] [ชื่อบัญชี]  ☐ ใช้งาน
พร้อมเพย์    [เบอร์/เลขประจำตัว] [ชื่อบัญชี]     ☐ ใช้งาน
QR           [อัปโหลดรูป]                        ☐ ใช้งาน
```

### โครงสร้างข้อมูลใน KV `pay:<shopId>`

```json
{
  "v": 2,
  "accounts": [
    { "bank": "<ชื่อธนาคาร>", "no": "<เลขบัญชี>", "name": "<ชื่อบัญชี>", "active": true }
  ],
  "promptpay": { "id": "<เบอร์หรือเลขประจำตัว>", "name": "<ชื่อบัญชี>", "active": false },
  "qr": { "url": "<ลิงก์รูป>", "active": false },
  "note": "<ข้อความเพิ่มเติมของร้าน>"
}
```

### ทางถอยหลัง (สำคัญ — ห้ามทำร้านเดิมพัง)

`payOf()` ต้องอ่านได้ทั้ง 2 แบบ:
- ถ้า parse เป็น JSON ได้และมี `v: 2` → ใช้โครงสร้างใหม่
- ถ้าไม่ได้ → เป็นข้อความอิสระแบบเดิม ใช้ heuristic เดิมต่อไป

ร้านที่ยังไม่ย้ายต้องทำงานได้เหมือนเดิม 100% โดยไม่ต้องแก้อะไร

---

## เกณฑ์ว่างานนี้เสร็จ

- [ ] ร้านตั้งได้หลายบัญชี และเลือกได้ว่าจะโชว์ใบไหน
- [ ] ร้านตั้งพร้อมเพย์ได้ · ปุ่มคัดลอกคัดลอกค่าที่ถูกต้อง
- [ ] ร้านอัปโหลด QR ได้ · การ์ดแสดงรูปได้
- [ ] ไม่มีการเดาโครงสร้างจากข้อความอิสระในเส้นทางใหม่
- [ ] ร้านที่ยังใช้ฟอร์แมตเดิมทำงานได้เหมือนเดิมทุกอย่าง (มีเทสกันถอยหลัง)
- [ ] เทสครอบ: หลายบัญชี · พร้อมเพย์อย่างเดียว · QR อย่างเดียว · ไม่ตั้งอะไรเลย · ฟอร์แมตเก่า

---

## สิ่งที่ **ไม่** อยู่ในขอบเขต Issue นี้

- Business Logic เรื่องจังหวะส่งข้อมูลชำระเงิน (นั่นคือ k188 — เสร็จแล้ว)
- การคิดยอด · `ordTotal()` · `computeOrder()`
- การตรวจสลิป / SlipOK
- ข้อมูลการรับเงินของร้านใดร้านหนึ่ง — **ห้ามใส่ลงโค้ด prompt เทส หรือเอกสารเด็ดขาด** ทุกอย่างมาจาก Config ต่อร้านเท่านั้น
