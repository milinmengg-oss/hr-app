# ISSUE · Menu Config V2

**สถานะ:** เปิดไว้ · ยังไม่แก้โค้ด · **ห้ามแก้ใน commit k188**
**เปิดเมื่อ:** 5 ส.ค. 2569 · พบตอนตรวจ Store-specific Data ก่อน commit k188
**ขอบเขต:** ลิงก์เมนูสินค้าของร้าน ซึ่งตอนนี้ hardcode อยู่ในโค้ด

---

## สรุปปัญหา

ลิงก์เมนูสินค้าเป็น **ข้อมูลเฉพาะร้าน (Store-specific Data)** แต่ตอนนี้ฝังอยู่ในโค้ดและ prompt
ที่ 1 ร้านใช้ได้ · ที่ 100 ร้าน **ทุกร้านจะส่งลิงก์เมนูของร้านแรกให้ลูกค้าตัวเอง**

เทียบกับข้อมูลรับเงินซึ่งทำถูกแล้ว (อ่านจาก KV `pay:<shopId>` ต่อร้าน) — ลิงก์เมนูยังไม่ได้ทำแบบเดียวกัน

---

## จุดที่ต้องย้ายไปเป็น Config ต่อร้าน

| # | ตำแหน่ง | ลักษณะ |
|---|---|---|
| 1 | `MENU_MSG` (~บรรทัด 858) | ข้อความคงที่ที่ระบบส่งเมื่อลูกค้าขอดูเมนู — ลิงก์ฝังอยู่ในสตริง |
| 2 | `SYSTEM_PROMPT` (~บรรทัด 2467) | ลิงก์เดียวกันฝังใน prompt ที่ส่งให้ AI ทุกครั้ง |
| 3 | ข้อความตอบตอนของหมด (~บรรทัด 6429) | `"(ดูครบทุกกลิ่นได้ที่เมนูค่ะ <ลิงก์>)"` — ต่อท้ายเมื่อกลิ่นเยอะเกินจะลิสต์หมด |

**⚠️ จุดที่ 2 อันตรายที่สุด** — อยู่ใน prompt แปลว่า AI "รู้จัก" ลิงก์นี้ และอาจพิมพ์ออกมาเองได้ในบริบทอื่น
ต่อให้แก้จุดที่ 1 กับ 3 แล้ว ถ้าไม่แก้จุดที่ 2 ลิงก์ก็ยังหลุดไปหาลูกค้าร้านอื่นได้อยู่ดี

---

## แนวทางที่เสนอ (ยังไม่ทำ)

### หลังบ้าน — เพิ่มช่องต่อร้าน

```
ลิงก์เมนูสินค้า  [__________________________]
```

### เก็บใน KV `menu:<shopId>`

ใช้แพทเทิร์นเดียวกับ `pay:<shopId>` ที่มีอยู่แล้ว — ไม่ต้องคิดกลไกใหม่

```js
async function menuOf(env, shopId) {
  // อ่าน KV "menu:<shopId>" · แคชสั้นๆ เหมือน payOf()
  // ไม่มี → คืนค่าว่าง แล้วให้ข้อความเมนูตัดส่วนลิงก์ออก
}
```

### กติกาเมื่อร้านยังไม่ตั้ง

- **ห้าม fallback ไปใช้ลิงก์ของร้านอื่นเด็ดขาด**
- ข้อความเมนูต้องยังส่งได้ แต่ตัดบรรทัดลิงก์ออก แล้วให้ระบบลิสต์สินค้าจากสต็อกจริงแทน
- prompt ต้องรับลิงก์เข้ามาเป็นตัวแปรต่อร้าน ไม่ใช่ค่าคงที่ในไฟล์

---

## เกณฑ์ว่างานนี้เสร็จ

- [ ] ไม่มีลิงก์เมนูของร้านใดอยู่ใน source code หรือ prompt เลย
- [ ] แต่ละร้านตั้งลิงก์เมนูของตัวเองได้จากหลังบ้าน โดยไม่ต้อง deploy
- [ ] ร้านที่ยังไม่ตั้งลิงก์ → ข้อความเมนูยังส่งได้ ไม่ส่งลิงก์ของร้านอื่น
- [ ] มีเทสยืนยันว่าร้าน A กับร้าน B ได้ลิงก์คนละอัน
- [ ] มีเทสยืนยันว่าร้านที่ไม่ตั้งลิงก์ ไม่ได้ลิงก์ของใครเลย

---

## สิ่งที่ **ไม่** อยู่ในขอบเขต Issue นี้

- Business Logic เรื่องจังหวะส่งเมนู
- เนื้อหาเมนู / รายการสินค้า / สต็อก
- ข้อมูลการรับเงิน → อยู่ใน Issue **Payment Config V2**
